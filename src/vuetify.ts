import { createVuetify } from 'vuetify';
import 'vuetify/dist/vuetify.min.css';

export default createVuetify({
    // Adicione as configurações específicas do Vuetify aqui
    theme: {
        defaultTheme: 'light'
    }
})
